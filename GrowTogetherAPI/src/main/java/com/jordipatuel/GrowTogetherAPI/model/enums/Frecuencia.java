package com.jordipatuel.GrowTogetherAPI.model.enums;

/** Frecuencia de repetición de un hábito. */
public enum Frecuencia {
    /** El hábito debe completarse todos los días. */
    DIARIO,
    /** El hábito solo aplica en los días de la semana definidos en diasSemana. */
    PERSONALIZADO
}
