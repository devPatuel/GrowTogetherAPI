package com.jordipatuel.GrowTogetherAPI.model.enums;
/** Roles disponibles en el sistema. */
public enum Roles {
    /** Usuario estándar con acceso a sus propios hábitos y desafíos. */
    STANDARD,
    /** Administrador con acceso al panel de gestión y auditoría. */
    ADMIN
}
